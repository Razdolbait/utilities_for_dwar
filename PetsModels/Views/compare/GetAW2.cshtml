@section metatags
{
	<meta name="robots" content="noindex">
}
<select id="aWeapon2" name="aWeapon2" class="btn-block DDL">
	@foreach (var item in Model)
	{
		<option value="@item.Id">@item.NAME</option>
	}
</select>